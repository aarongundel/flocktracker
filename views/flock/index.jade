h1 Flocks
p
  a(href='/flocks/new', class='btn primary') Add a Flock
- if(flocks.length)
  table
    tr
      th Flocks
        each flock in flocks
          tr
            td #{flock.name}
            td #{flock.organizer}
            td #{flock.shortID}
            td #{flock.location}
            td
              a(href="/flocks/#{flock.id}/edit") Edit
- else
  p There are no flocks created yet. :(